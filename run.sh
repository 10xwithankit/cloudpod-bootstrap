#!/bin/bash

# ⚠️ This is a placeholder run script
# This cloudpod-bootstrap repo is meant to set up your cloud server environment.
# To actually run your application (FastAPI, worker, trainer, etc.),
# clone your app repo and run its own custom run.sh

echo "⚠️ No app-specific run.sh found."
echo "👉 You must clone and run your application's launcher. For example:"
echo "   git clone https://github.com/your-org/fastapi-core-api.git /workspace/app"
echo "   cd /workspace/app && bash run.sh"
